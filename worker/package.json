{
  "name": "@automations/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types --env-interface WorkerEnv",
    "typecheck": "tsc --noEmit",
    "test": "vitest run && node --experimental-strip-types --test test/*.test.mjs",
    "test:workers": "vitest run",
    "test:node": "node --experimental-strip-types --test test/*.test.mjs"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@types/node": "^26.4.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.11",
    "wrangler": "^4.127.1"
  },
  "dependencies": {
    "zod": "^3.25.76"
  }
}
